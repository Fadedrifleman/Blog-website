<%- include("partials/header"); -%> 

    <h1>Home</h1>
    <p><%= homeContent %> </p>

<% posts.forEach(post => { %>
    <h1><%= post.title %> </h1>
    <p><%= post.content.substring(0,100) + "..." %> <a href="/posts/<%= post.title %> ">Read more</a> </p>
<% }); %>

    <div class="allign-button">
        <a href="/compose">
            <button class="btn compose-button">+</button>
        </a>
    </div>

<%- include("partials/footer"); -%> 